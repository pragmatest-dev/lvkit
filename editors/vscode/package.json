{
  "name": "lvkit-vi",
  "displayName": "lvkit — LabVIEW VI Viewer & Diff",
  "description": "View LabVIEW .vi block diagrams and see visual before/after diffs in VS Code — no LabVIEW license required.",
  "version": "0.1.0",
  "publisher": "pragmatest",
  "license": "Apache-2.0",
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Visualization", "SCM Providers", "Other"],
  "keywords": ["labview", "vi", "diff", "block diagram", "ni", "test"],
  "activationEvents": ["onCommand:lvkit.diffVI", "onCommand:lvkit.generatePython", "onCustomEditor:lvkit.viPreview"],
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "title": "lvkit",
      "properties": {
        "lvkit.path": {
          "type": "string",
          "default": "lvkit",
          "markdownDescription": "Path to the `lvkit` executable. Use just `lvkit` if it is on your PATH, or an absolute path (e.g. a project venv's `.venv/bin/lvkit`)."
        },
        "lvkit.searchPaths": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "markdownDescription": "Extra directories to search for SubVIs, in addition to the auto-detected repository root."
        },
        "lvkit.diagramTheme": {
          "type": "string",
          "enum": ["auto", "light", "dark"],
          "enumDescriptions": [
            "Follow the editor theme — light diagram in a light theme, dark in a dark theme.",
            "Always render the VI diagram with the light LabVIEW palette, regardless of editor theme.",
            "Always render the VI diagram with the dark palette, regardless of editor theme."
          ],
          "default": "auto",
          "markdownDescription": "Color theme for the **VI diagram itself** (the rendered block diagram and diff SVGs). The surrounding viewer chrome always matches your VS Code theme; this setting controls only the diagram. `auto` (default) makes the diagram follow the editor theme; `light`/`dark` pin it regardless."
        }
      }
    },
    "commands": [
      { "command": "lvkit.diffVI", "title": "Open Visual Diff", "category": "lvkit" },
      { "command": "lvkit.generatePython", "title": "Convert VI to Python (beta)", "category": "lvkit" }
    ],
    "customEditors": [
      {
        "viewType": "lvkit.viPreview",
        "displayName": "lvkit VI Preview",
        "selector": [{ "filenamePattern": "*.vi" }],
        "priority": "default"
      }
    ],
    "menus": {
      "scm/resourceState/context": [
        { "command": "lvkit.diffVI", "when": "resourceExtname == .vi", "group": "navigation@1" },
        { "command": "lvkit.generatePython", "when": "resourceExtname == .vi", "group": "navigation@2" }
      ],
      "explorer/context": [
        { "command": "lvkit.diffVI", "when": "resourceExtname == .vi", "group": "navigation@1" },
        { "command": "lvkit.generatePython", "when": "resourceExtname == .vi", "group": "navigation@2" }
      ],
      "editor/title/context": [
        { "command": "lvkit.diffVI", "when": "resourceExtname == .vi" },
        { "command": "lvkit.generatePython", "when": "resourceExtname == .vi" }
      ]
    }
  }
}
